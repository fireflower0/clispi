(uiop:define-package #:<% @var name %>/app
  (:use #:cl
        #:<% @var name %>/wrapper/wiringpi)
  (:export #:main))
(in-package #:<% @var name %>/app)

(defun main ()
  ;; Initialization
  (loop
     ;; Your progra
     ))
